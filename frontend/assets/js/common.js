import { initializeSocket } from './socket/socket-client.js';

document.addEventListener('DOMContentLoaded', () => {
    const socket = initializeSocket();
    
    // You can add more common functionality here if needed
});